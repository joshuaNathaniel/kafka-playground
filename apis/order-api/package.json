{
  "name": "order-api",
  "version": "1.0.0",
  "description": "This project was bootstrapped with Fastify-CLI.",
  "main": "app.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "tap \"test/**/*.test.js\"",
    "start": "TOPIC=${TOPIC:-ORDER} KAFKA_BOOTSTRAP_SERVER=${KAFKA_BOOTSTRAP_SERVER:-localhost:9092} fastify start -l info app.js -p ${PORT:3000}",
    "dev": "TOPIC=${TOPIC:-ORDER} KAFKA_BOOTSTRAP_SERVER=${KAFKA_BOOTSTRAP_SERVER:-localhost:9092} fastify start -w -l info -P app.js -p ${PORT:3000}"
  },
  "keywords": [],
  "author": "Joshua Nathaniel Miller",
  "license": "MIT",
  "dependencies": {
    "fastify": "^3.0.0",
    "fastify-autoload": "^3.10.0",
    "fastify-cli": "^2.15.0",
    "fastify-plugin": "^3.0.0",
    "fastify-sensible": "^3.1.2",
    "kafkajs": "^1.16.0",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "tap": "^15.1.6"
  }
}
